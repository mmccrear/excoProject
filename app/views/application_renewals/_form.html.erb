<div class = "container">
<h1>Course Renewal Application</h1>
<br>
<%= form_for(@application_renewal) do |f| %>
  <% if @application_renewal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@application_renewal.errors.count, "error") %> prohibited this application_renewal from being saved:</h2>

      <ul>
      <% @application_renewal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
    <label for="credits">
    <b>For how many credits should this course be offered?</b><br>
       Note that two credit courses are much more rigorous than one credit courses (refer to course guidelines for more information). New courses are rarely allowed to be taught for two credits.
    </label>
    <%= f.select :credits, options_for_select([1,2],1)%><br><br>
  </div>

  <div class="field">
    <label for = "capactiy">What should be the maximum number of students in your class?</label><br>
    <%= f.number_field :capactiy %><br><br>
  </div>

  <div class="field">
    <label for = "problems">What were the major problems with this class the last time it was offered? How will you try to solve them for next semester?</label><br>
    <%= f.text_area :problems %><br><br>
  </div>

  <div class="field">
    <label for = "dbowie">What changes are you making to the course, if any?</label><br>
    <%= f.text_area :dbowie %><br><br>
  </div>

  <div class="field">
    <label for = "engagement">How do you think that the students felt about this course the last time it was taught? Were the students engaged in the course? Do you think that all students felt comfortable with the course material?</label><br>
    <%= f.text_area :engagement %><br><br>
  </div>

  <%= f.hidden_field :instance_id, :value=>params[:instance_id] %>
  <div class="actions">
    <%= f.submit class: 'btn btn-primary' %>
  </div>
<% end %>
</div>
